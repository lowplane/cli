// Package rules holds the deterministic detector library: cost detectors
// (overprovisioned CPU/mem, missing limits, etc.) and security detectors.
// All detectors are pure functions over the parser's representation.
package rules
